# A zero value selects the configured YAML default.
# Nonzero scale values must be in the range (0, 1].
# A zero timeout_sec selects the default timeout of 60 seconds.
float64 velocity_scale
float64 acceleration_scale
float64 jerk_scale
float64 timeout_sec
